{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with g++-15",
            "type": "shell",
            "command": "/usr/local/bin/g++-15",
            "args": [
                "-std=c++23",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
